#!/usr/bin/env bash

# Restarts the process that's not reliable.
# Good for running accidentally crashing servers.

trap 'exit 0' INT TERM

while true; do
  eval "$@"
  if [[ "$?" == "0" ]]; then
    exit 0
  fi
done
